## 链表

头指针 + 节点 + 结尾
头指针（虚拟节点）：设置数值为 -1，指针指向下一个

链表成环，只要最后的一个指针指向第一个 那么链表就成环了，快慢指针从而判断链表是否成环，如果快的等于慢的话那么就说明成环了

双向链表 有两个指针域 一个指向前面 一个指向后面

链表创建，先创建一个 node 然后赋值，然后再创建一个指针来进行遍历 p.next = node; p = p.next; // 链表节点前进

整表删除 依次指向下一个节点然后释放节点空间

